[phases.setup]
aptPkgs = ["...", "tesseract-ocr", "tesseract-ocr-eng", "poppler-utils"]

[phases.build]
cmds = [
  "which tesseract && tesseract --list-langs | grep -q '^eng$'",
  "pip install -r requirements.txt"
]
